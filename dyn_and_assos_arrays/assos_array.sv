module assos_array;
// ASSOSIATIVE  arrays are not supported  by icarus use riviera
/*
int scoreboard[int];

initial begin
    scoreboard[10] = 100;
    scoreboard[20] = 200;
    scoreboard[30] = 300;

    if (scoreboard.exists(20))
        $display("scoreboard[20] = %0d", scoreboard[20]);

    foreach (scoreboard[k])
        $display("key=%0d value=%0d", k, scoreboard[k]);

    $display("entries = %0d", scoreboard.num());

    scoreboard.delete(20);

    $display("after delete:");
    foreach (scoreboard[k])
        $display("key=%0d value=%0d", k, scoreboard[k]);
end

*/
endmodule